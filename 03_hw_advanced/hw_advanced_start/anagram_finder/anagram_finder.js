const AnagramFinder = function (word) {
    this.word = word.toLowerCase()

}

AnagramFinder.prototype.findAnagrams = function (otherWords) {

    //should test if the letters in word has the same letters as (comparison?)
    //must be same length as comparison 
    //would need to use split again and a comparrison 
    //but would have to put the string back together again

}

module.exports = AnagramFinder;
